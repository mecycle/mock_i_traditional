const users = [
  { id: 1, name: 'Alice', active: true },
  { id: 2, name: 'Bob', active: false },
  { id: 3, name: 'Charlie', active: true }
] 

const result = users
  .filter(user => user.active)
  .map(user => user.name)

const orders = [
  { userId: 1, amount: 100 },
  { userId: 1, amount: 50 },
  { userId: 2, amount: 80 }
]

Calculate total spend per user.
const totals = orders.reduce<Record<number, number>>((acc, order) => {
  acc[order.userId] = (acc[order.userId] ?? 0) + order.amount
  return acc
}, {})
